<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>Strickland Realty</title>
    <link rel="stylesheet" href="{{ url_for('static', filename='style.css') }}" type="text/css">
  </head>
  <body>
    <div class="container">
    <ul class="menu">
      <li class="menuItem"><a class="menuItemLink" href="index">Home</a></li>
      <li class="menuItem"><a class="menuItemLink" href="listings">Listings</a></li>
      <li class="menuItem"><a class="menuItemLink" href="realtors">Realtors</a></li>
      <li class="menuItem"><a class="menuItemLink" href="buyers">Buyers</a></li>
      <li class="menuItemactive"><a class="menuItemLink" href="sellers">Sellers</a></li>
      <li class="menuItem"><a class="menuItemLink" href="realtors-buyers">Realtors-Buyers</a></li>
    </ul>
    </div>
    <div>
      <h1>Sellers</h1>
      <p>This page will allow you to update sellers.</p>
    </div>
    <div>
      <h2>Update a Seller</h2>
      <form id="updateseller" action="/sellers-update" method="post">
        <fieldset>
          <legend>Update Seller</legend>
          <p>    
            <label>
              Seller ID: {{ context['Seller'].SellerID }}
            </label>
          </p>
          <p>
            <label>
              First Name:
              <input type="text" name="fname_update" size="30" maxlength="100" value="{{ context['Seller'].FirstName }}" required>
            </label>
            <label>
              Last Name:
              <input type="text" name="lname_update" size="30" maxlength="100" value="{{ context['Seller'].LastName }}" required>
            </label>
          </p>
          <p>
            <label>
              Email:
              <input type="text" name="email_update" size="30" maxlength="100" value="{{ context['Seller'].Email }}" required>
            </label>
            <label>
              Phone Number:
              <input type="tel" name="phone_update" size="30" maxlength="100" 
              pattern="[0-9]{3}[0-9]{3}[0-9]{4}" value="{{ context['Seller'].Phone }}" required>
            </label>
          </p>
          <p>
            <label>
              Realtor:
              <select name="realtor_update" id="realtors">
                  {% for Realtor in context['Realtors'] %}     
                     {% if context['Seller'].RealtorID == Realtor['RealtorID'] %}              
                     <option selected value="{{Realtor['RealtorID']}}">Name: {{Realtor['FirstName']}}, ID: {{Realtor['RealtorID']}}</option>
                     {% else %}
                     <option value="{{Realtor['RealtorID']}}">Name: {{Realtor['FirstName']}}, ID: {{Realtor['RealtorID']}}</option>
                     {% endif%}
                  {% endfor %}
                </select>
            </label>
          </p>
          <p>
            <button class="btn" type="submit" name="update_button" value="{{ context['Seller'].SellerID }}">Update Seller</button>
          </p>
        </fieldset>
      </form>
    </div>
  </body>
</html>